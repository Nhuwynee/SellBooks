<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- Đặt icon cho mục Add book -->
    <item
        android:id="@+id/navigation_add_book"
        android:icon="@drawable/icon_addbook"
        android:title="Thêm sách" />

    <!-- Đặt icon cho mục List books -->
    <item
        android:id="@+id/navigation_list_books"
        android:icon="@drawable/ic_list_books"
        android:title="Danh sách sách" />
    <item
        android:id="@+id/navigation_audio"
        android:icon="@drawable/ic_audio"
        android:title="Sách nói" />

    <!-- Đặt icon cho mục Edit -->
    <item
        android:id="@+id/navigation_orders"
        android:icon="@drawable/ic_orders"
        android:title="Đơn hàng" />

    <!-- Đặt icon cho mục Orders -->
    <item
        android:id="@+id/navigation_account"
        android:icon="@drawable/ic_account_admin"
        android:title="Tôi" />

</menu>
